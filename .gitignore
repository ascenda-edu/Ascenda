node_modules
.next
out
# Environment secrets — keep every variant out of git (.env.example is the only tracked one)
.env
.env.*
!.env.example
tsconfig.tsbuildinfo
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Confidential research
Interview Transcripts/
transcripts/

# Generated import CSVs (recreated by scripts/all-countries-to-supabase.ts)
supabase/imports/**/*.csv
.vercel

# Supabase CLI local state (project ref, pooler URL, versions)
supabase/.temp/

# Playwright: HTML report, per-run artifacts (traces/screenshots), and the
# signed-in storage state written by e2e/auth.setup.e2e.ts — that last one is a
# live session cookie and must never be committed.
playwright-report/
test-results/
.playwright/
